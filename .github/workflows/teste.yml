name: agora tenho nome

on: push

env:
  MINHA_ENV: valor que a gente quiser

jobs:
  job-linux:
    env:
      JOB_ENV: 1
    runs-on: ubuntu-latest
    steps:
      - name: meu step
        env: 
          STEP_ENV: 2
        run: |
          echo $MINHA_ENV
          echo $JOB_ENV
          echo $STEP_ENV