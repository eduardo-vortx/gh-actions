name: agora tenho nome

on: push

env:
  MINHA_ENV: valor que a gente quiser

jobs:
  job-linux:
    env:
      JOB_ENV: 1
    runs-on: ubuntu-latest
    steps:
      - name: meu step
        run: |
          node -v
          npm -v
          python -v
          git --version